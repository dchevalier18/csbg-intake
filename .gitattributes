# Keep server-side scripts and config LF-only so they run on the Linux dev box
# (the Linux server) regardless of the Windows checkout's autocrlf setting.
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
