# Keep server-side scripts and config LF-only so they run on the Linux dev box
# (YOUR_SERVER_IP) regardless of the Windows checkout's autocrlf setting.
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
